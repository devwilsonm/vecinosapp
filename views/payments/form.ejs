<%- include("../partials/header", { title: "Registrar pago | VecinosApp" }) %>
<section class="page-head"><h1>Registrar pago</h1></section>
<%- include("../partials/errors", { errors }) %>
<div class="detail">
  <p><strong>Ocupante:</strong> <%= allocation.full_name %></p>
  <p><strong>Recibo:</strong> <%= allocation.receipt_number %> · <%= allocation.period %></p>
  <p><strong>Saldo pendiente:</strong> S/ <%= money(allocation.balance_cents) %></p>
</div>
<form method="post" class="form">
  <input type="hidden" name="_csrf" value="<%= csrfToken %>">
  <label>Monto pagado <input type="number" step="0.01" min="0.01" max="<%= money(allocation.balance_cents) %>" name="amount" value="<%= formData.amount %>" required></label>
  <label>Fecha de pago <input type="date" name="payment_date" value="<%= formData.payment_date %>" required></label>
  <label>Método de pago
    <select name="payment_method" required>
      <option value="">Seleccionar</option>
      <option>efectivo</option>
      <option>transferencia</option>
      <option>yape/plin</option>
      <option>otro</option>
    </select>
  </label>
  <label>Observación <textarea name="note"></textarea></label>
  <div class="form-actions"><button class="button" type="submit" title="Registrar pago"><svg class="action-icon" viewBox="0 0 24 24" aria-hidden="true"><rect x="3" y="6" width="18" height="12" rx="2"></rect><path d="M3 10h18"></path><path d="M7 14h3"></path></svg><span>Registrar pago</span></button><a href="/payments">Cancelar</a></div>
</form>
<%- include("../partials/footer") %>